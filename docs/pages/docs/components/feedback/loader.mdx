# Loader

Animated spinner for indicating loading state

import { LoaderPlayground, LoaderExample, properties } from '@examples/loader'
import PropsTable from '@components/props-table'
import { UnfoldVariants } from '@components/unfold-variants'
import { ELEMENT_SIZES } from '@creation-ui/react'

<LoaderPlayground />

### Customisation

Loader can be modified by using `className` property:

- text color styling to change circle background
- fill color to change the short spinning part

```tsx copy
<LoaderExample cx={{ inner: 'fill-white text-red-800' }} />
```

<div className={'bg-black p-3 rounded-md'}>
  <LoaderExample cx={{ inner: 'fill-white text-red-800' }} />
</div>

## Component API

<PropsTable properties={properties} name='Loader' description='Loader component props' />
