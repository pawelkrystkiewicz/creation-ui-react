import PropsTable from '@/components/props-table'
import { labelProperties } from '@/examples/input-field'

# Label

Display a label for the input field. For example checkout usage in [Field](field) component.

## Components API

<PropsTable
  properties={labelProperties}
  name='Label'
  dependency={{
    name: 'Headless UI Input',
    link: 'https://headlessui.com/react/input',
  }}
/>
