import PropsTable from '@/components/props-table'
import { errorProperties } from '@/examples/input-field'

# Error

Display an error message for the input field. For example checkout usage in [Field](field) component.

## Components API

<PropsTable
  properties={errorProperties}
  name='Error'
  dependency={{
    name: 'Headless UI Input',
    link: 'https://headlessui.com/react/input',
  }}
/>
