import { Tabs } from 'nextra/components'

## Instalation

This component is part of the main `@creation-ui/react` package. To install it just run:

<Tabs items={['yarn', 'npm', 'pnpm', 'bun']}>
  <Tabs.Tab>

    ```bash copy
     yarn add @creation-ui/react
     ```

  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
     npm i @creation-ui/react
     ```
     </Tabs.Tab>
</Tabs>
